services:
  videoget:
    build: .
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DOWNLOAD_DIR: "/app/downloads"
      DOUYIN_COOKIE: "${DOUYIN_COOKIE:-}"
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
